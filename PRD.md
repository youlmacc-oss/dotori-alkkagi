# [PRD] 도토리 알까기 (Dotori Alkagi 3D)

## 1. 프로젝트 개요
* **목표**: 쿼터뷰 3D 원목 바둑판과 조약돌 형태의 입체 알을 조작하는 직관적인 웹 기반 알까기 게임.
* **핵심 타깃 환경**: 모바일 웹 뷰포트 최적화 (세로 모드 기준 터치 드래그 슈팅).
* **기술 스택**: Three.js (3D 렌더링), Matter.js (2D 물리 엔진 연동), Web Audio API (자체 합성 효과음), Vite.

## 2. 시각 & 렌더링 명세 (Three.js)
* **바둑판**:
  - 카메라: 44~46도 모바일 최적 다이내믹 쿼터뷰.
  - 판면: 어두운 음영 버그가 없는 밝고 따뜻한 비자나무 톤 (`#f3cf92` 기반 나뭇결 및 선명한 9x9 각인 격자선).
  - 프레임: 다크 월넛 외곽 베젤 및 하단 원형 펠트 매트.
* **바둑알**:
  - 형태: 단순 원기둥이 아닌 납작하고 둥근 조약돌 형상 (`SphereGeometry` 스케일링).
  - 질감: 흑단(유광 흑요석) 및 백옥 세라믹 반사광과 부드러운 하이라이트.
* **타격감 연출**:
  - `spawnPop`: 알 충돌 시 충돌 지점에 팽창하는 파티클 팝 이펙트.
  - `spawnShake`: 강한 타구 시 카메라 미세 진동(Camera Shake)으로 묵직한 손맛 전달.
  - `spawnFall`: 장외 탈락 알 부드러운 낙하 연출.

## 3. 오디오 명세 (Web Audio API)
* 외부 MP3 의존 없이 브라우저 자체 오디오 컨텍스트 합성:
  - `playStoneHit(intensity)`: 충돌 속도에 비례하는 맑고 단단한 "딱!" 타구음.
  - `playFlick(power)`: 발사 릴리즈 시 경쾌한 핑 효과음.
  - `playFall()`: 장외 탈락 시 목재 판 아래로 굴러떨어지는 저음 효과음.

## 4. 모바일 UI/HUD 명세 (`index.html`)
* **상단 HUD**: 알 지갑 카운트, 턴 제한시간 링 타이머, 모드 전환(AI 연습전 / 1:1 대전), 환경설정(⚙️).
* **하단 조작부**: POWER 충전 게이지, 조준 가이드선 토글 버튼(🎯), 하단 오터치 방지 겸용 경기 상황 전광판(Ticker Billboard).
* **대기실 & 관람 (단순화)**:
  - 우측 👥 대기실 버튼 클릭 시 진행 중인 대국 방 목록 팝업.
  - [관람하기] 선택 시 상단에 🔴 라이브 관람 배지 노출 및 입력 잠금.

## 5. 온라인 1:1 방 (권위는 방 상태 하나)
* **정원**: 대기실 동시 접속 10명. 닉네임 최대 5글자.
* **1인·AI**: 로컬만 둔다. 온라인 동기 없음.
* **방 상태**: `{ roomId, hostId, guestId, inviteTargetId, started, phase }`. 호스트가 쓰고, 화면은 이 값만 본다.
* **개설**: 호스트가 1:1을 열면 `room_{hostId}`가 생기고 `guestId`는 비어 있다. 혼자 있어도 AI로 바뀌지 않는다. 10분 미시작이면 대기실로 돌아간다.
* **대기실**: Presence는 누가 대기·어느 방을 열었는지만 알린다. 참가하기는 대기 중인 1:1에만 있다. 열린 1:1은 목록에 보이고 `초대손님을 기다리는 중`이라고 안내한다. 접속 후 가이드북을 닫거나 튜토리얼을 끝내거나 건너뛰면 초대만 가능하다는 안내를 한 번 띄운다.
* **초대**: 호스트가 대기 중인 사람에게 보낸다. 대상에게 수락/거절이 뜬다. 수락하면 `guestId`가 그 사람이 되고 호스트 초대 시트는 닫힌다. 거절·만료·다른 사람 재초대면 이전 초대는 무효다. 참가는 한 명만 되고, 늦은 쪽은 대기실로 돌아간다.
* **시작**: `guestId`가 있을 때만 시작 가능. 도토리가 적은 쪽이 선공. 선공이 시작하면 `started: true`이고 후공은 따른다. 흑은 호스트, 백은 게스트.
* **대국**: 같은 방의 돌·턴·승패는 호스트가 보내는 판 스냅샷 하나다.
* **퇴장**: 시작된 판에서 나가거나 창을 닫거나 연결이 끊기면 기권. 남은 사람은 승을 정산한다. 대기 중 퇴장은 누른 사람만 나간다. 남은 사람은 상대 대기(`guestId` 없음)로 돌아간다. 호스트가 나가면 남은 사람이 자기 방을 연다. 다시하기는 같은 방을 유지한다. 대국 중 설정 적용은 불가. 이미 시작된 방에는 참가할 수 없다.
* **관전**: 1:1만 본다. 1인·AI는 관람하지 않는다. 입력 잠금. 방 상태와 판 스냅샷만 본다. 끝난 1:1은 목록에 대국 종료로 보인다. 대기실이 가득 차면 11번째는 접속하지 않는다.

## 6. 제외 기능 (Out of Scope)
* **텍스트 채팅 및 이모지 시스템 완전 배제**: 모바일 가상 키보드 화면 가림 및 성능 저하 방지.
* **토너먼트/리그전 시스템 배제**: 단판 대결 및 가벼운 방별 관람 기능에 집중.
* **계정·랭킹·매칭 서버 배제**: 지인 10명 비공개 링크/대기실 초대만.
