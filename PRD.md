# [PRD] 도토리 알까기

이 문서만으로 제품을 다시 만들 수 있어야 한다. 화면 수치는 `UI_PROMPTS.md`, 모듈·상수·이벤트·식은 `ARCHITECTURE.md`, 작업 절차는 `ALL-IN-ONE PRODUCTION PROTOCOL.md` (`UNIVERSAL PRODUCTION PROTOCOL.md`와 동일 계열).

다른 PC에서 재현할 때 **이 네 문서 밖의 기획을 만들지 않는다.** 코드에 남은 1:1 스텁은 제품 기능이 아니다.

## 1. 제품 잠금 (재협상 금지)

- **이름**: 도토리 알까기. HTML `<title>`과 대기실 표제(`#lobby-book-open`)는 이 문자열만.
- **장르**: 웹 바둑알 까기. 쿼터뷰 3D 원목 판 + 납작한 조약돌 알. 터치를 뒤로 당겨 놓으면 반대 방향으로 발사.
- **플레이 인원**: **1인 연습 + AI 대국만**. 1:1 PVP는 제품에서 제외. 로비 `#lobby-mode-pvp`, 설정 `[data-mode=pvp]`, HUD `[data-mode=pvp]`는 `hidden` + `display:none`. `startOwnPvpRoom` / 설정 1:1 탭은 no-op. 초대·관전·공개 방 참가·대기실 봇 좌석은 제공하지 않는다.
- **호스팅**: GitHub Pages만. 라이브 `https://youlmacc-oss.github.io/dotori-alkkagi/`. 저장소 `https://github.com/youlmacc-oss/dotori-alkkagi`. Vercel 금지.
- **공개**: 저장소 public. `main` 푸시가 `dist`를 Pages에 올린다. `GITHUB_PAGES=1`이면 Vite `base` `/dotori-alkkagi/`.
- **타깃**: 모바일 세로 터치. 데스크톱은 같은 HUD를 가운데 540px 스테이지에 담는다.
- **언어**: UI·티커·가이드·점검·결과는 한국어.
- **스택**: Vanilla ESM. React/Vue 없음. Vite 7 + Matter.js + Three.js + Howler. 엔트리 `src/main.js`. 본판 렌더는 `ThreeRenderer`만 (`CanvasRenderer`는 쓰이지 않음).

## 2. 한 판의 규칙

- **판**: 가상 월드 720×1280. 바둑판 외곽 640×640 (원점 40,40), 테 28, 내면 584×584 (원점 68,68). 9×9 격자(선 인덱스 0–8). 흑은 아래, 백은 위. 중력 0.
- **알**: 한쪽 기본 5알. 설정에서 3 / 5 / 7 / 9. 반지름 24. 3D 시각 배율 1.14, 구를 Y 0.46으로 납작하게. 집기 판정은 반지름 × 1.52.
- **색**: 사람은 흑(아래). AI는 백(위). 1인은 흑·백을 번갈아 직접 쏜다 (`myColor = currentTurn`). 선공은 항상 흑.
- **턴**: 15초. 5초 남으면 타이머 긴급색 + 틱음. 시간 초과면 조준 취소, 발사 없이 상대 턴.
- **조준**: 자기 색 살아 있는 알만. 뒤로 당긴 거리로 파워. 당김 데드존 14, 최대 당김 360, 최대 발사속 54. 감도 1.0–6.0 (기본 3.8, 0.1). 발사 속도 = min(당김거리×감도×0.025, 54), 방향은 당김의 반대.
- **당김 금지**: 붙어 있는 돌 방향(근거리 14, 40° 코사인), 다른 돌 위로 당기기. `붙어 있는 돌 방향으로는 당길 수 없습니다.` / `바둑돌위로 당길수 없습니다`.
- **정지**: 속력 0.05·각속 0.04 미만이 18프레임이면 턴 종료.
- **장외**: 알 중심이 나무판 시각 범위(`BOARD_VISUAL` MESH 480, INSET 68) 밖이면 탈락. 격자선이 아니라 나무판 끝.
- **승패**: 한쪽 생존 0이면 상대 승. 동시 0이면 무승부. 기권은 한 탭, 기권한 쪽 패.
- **결과**: 마지막 낙사 연출 뒤 연다 (낙사 홀드 1920ms, 박자 420ms). 한 판 더 / 대기실.

## 3. 모드와 시작 흐름

### 3-1. 대기실 → 대전방

1. 사이트 열면 대기실(`#lobby-users`). 가이드북은 첫 세션에 자동 오픈. `dotori-alkkagi-book-skip`이면 자동 오픈 안 함.
2. **1인** 또는 **AI**를 누르면 대전방. `setMatchConfig`가 일자를 깔아도 직후 저장 진형(`play-formation`)을 복구한다.
3. 재배치 질문 5초: `바둑돌을 재배치 하시겠습니까?` 예/아니오. 예면 10초 재배치(첫째 선 안, 자기 진영). 설정에서 재배치 질문을 끄면 시작 버튼이 바로 나온다.
4. 선공 안내(질문 후 3초): `호스트(흑)가 먼저입니다. 선공이 시작 버튼을 누릅니다.`
5. 시작 전 HUD 조준선과 **같은 줄**에 알 수 **3알 / 5알 / 7알 / 9알** 과 진형 **일자형 / 쐐기형 / 방어형**. 고르면 본판이 즉시 바뀌고 `play-formation`에 저장된다. 시작 후 숨김. 기권·대기방·조준선 50×50 불변. 좁은 화면에서는 선택 칩만 줄여 한 줄을 지킨다.
6. 시작 → 1인·AI는 피어 없이 `applyMatchStarted`. 앰비언스 match. `playStart`.

### 3-2. 1인

- 흑·백 모두 사람. 호스트(흑) 선공.
- 도토리 정산 없음.
- 튜토리얼은 1인만: 잡기 → 당기기 → 발사 → 완료. 가이드북 `튜토리얼 시작` / 도크 버튼. 도토리 안 건드림.

### 3-3. AI

- 아래 흑=사람, 위 백=봇. 흑 선공. 백 턴에는 사람 입력 잠금.
- 난이도 초급 / 중급 / 고급. **기본·저장 기본값은 고급** (`dotori_ai_difficulty_v2`).
- 세 난이도 조준 오차 0°. 히트 임베드 12 / 14 / 18. 당김 금지·첫 충돌을 통과한 샷만.
- 생각 1.0–1.5초, 조준 표시 0.5초. 타이머가 이미 있으면 재시작 금지.
- AI 대국만 도토리 정산: 승 +1, 패 −1, 0 미만 허용. 무승부·1인 없음.

### 3-4. 한 판 더 / 대기실

- 한 판 더(솔로/AI): 같은 모드 대전방 재입장 + `setMatchConfig` + 저장 진형 복구.
- 대기실: 로비로 복귀, 엔진 일시정지, 닉·모드 버튼 유지.

### 3-5. 1:1 (제품 오프)

화면에서 고를 수 없다. 초대 시트·가이드선 질문·관전 바는 DOM에 남아도 제품 경로가 열지 않는다. `openPvpGuidePick`은 false. `shouldOfferInviteOnlyNotice`는 항상 false.

## 4. 대기실 (솔로 시작 메뉴)

- 표제 버튼 `#lobby-book-open`: **도토리 알까기**.
- 힌트 `#lobby-location-guide`: `1인 연습 · AI 대국` (문자열에 `1:1` 없음).
- 버튼: 1인, AI(기본 is-on). 가이드북, 점검, 설정 ⚙️, ✕/게임종료.
- 닉 maxlength 5. 비우면 `도토리N`(접속 순). 중복이면 접미 또는 다음 `도토리N`. 힌트 `닉네임은 5글자 이내` / `이미 있는 닉이라 다른 이름으로 저장했습니다`. 대전·관람 중 변경 금지.
- 사이트 접속 표시 상한 10명. `canAdmitUser`: 이미 목록에 있거나 length < 10.
- 같은 탭(F5)만 닉·도토리·유저 id 유지. 영구 전적 없음.
- 부팅 시 **지운다**: localStorage `dotori-alkkagi-acorns`, `dotori-alkkagi-nickname`(영구), `dotori-alkkagi-scene-bg`. **지우지 않는다**: `dotori-alkkagi:visit-log`.
- 가이드북 7장(cover/pull/modes/ready/acorn/fall/sound). 체크 `다음 접속시에는 이 창을 띄우지 않음`. `바로시작`(현재 모드로 대전 입장).
- 점검 탭: 물리·1인·AI·조준선·설정 항목. `1:1 참가 배제` 문구는 쓰지 않는다.

## 5. 진형

- 설정 배치: 프리셋(누를 때마다 임의) / 직접 놓기 / 내 진형. 슬롯별 진형 저장·열기.
- 칩: 3알 일자·쐐기·종대. 5/7/9 일자·쐐기·방어. 1알은 대치(제품 칩에 안 나옴).
- **저장(적용)** 은 현재 미리보기를 본판과 `dotori-alkkagi:play-formation`에 올린다. 일자로 되돌리면 안 된다.
- 대전 입장·한 판 더(솔로/AI)도 저장 진형을 다시 깐다.
- 프리셋 존 진영 높이 30%. 직접/내 진형·재배치는 첫째 선(백 1 · 흑 7) 안.
- `setMatchConfig`는 테스트 때문에 LINE을 깐다. 호출 측이 직후 저장 진형을 복구한다.

## 6. HUD·회전·설정

- 상단: 🌰 지갑, 먼 좌석 닉, 15초 링 + `흑 턴`/`백 턴`.
- 하단 줄: 기권 · 대기방 · 조준선(🎯 ON/OFF). 시작 전 3알·5알·7알·9알 · 일자형·쐐기형·방어형. POWER 50px 중앙. 티커.
- 왼쪽 턴 = 카메라 −45°, 오른쪽 턴 = +45°. 빈 판 짧은 탭 = **시계 +45°만**. Matter·조준·턴은 불변. 조준·RESOLVING·킬캠에서 비활성.
- 설정: 톤 4종+색조·밝기, 감도, 음량·뮤트, 액션캠, 재배치 질문, 조준선 사용/색, 모드·난이도·알 수·진형·배치, **이력**.
- 대국 시작 후 저장의 대기실 적용 금지: `대국 중에는 적용할 수 없습니다`. 미리보기·이력 조회는 가능.
- 접속 이력: 조회만. 앱이 어떤 경우에도 지우지 않음(재배포·캐시 소거·설정 저장·퇴장 포함).

## 7. 도토리·세션

- 세션 시작 10. AI 승패만 ±1. 1인 변동 없음. 창을 닫으면 다시 10.
- Night session: 같은 탭 F5는 id/닉/도토리 유지. 다른 탭이 8초 안에 같은 id claim이면 새 id.

## 8. 결과·종료 카피

| 조건 | 제목 | 부제 |
| --- | --- | --- |
| 무승부 | 무승부 | 양측이 동시에 장외로 나갔습니다 |
| AI 흑 승 | 승리! | AI 돌이 전멸했습니다 |
| AI 백 승 | 패배 (AI 승리) | 내 돌이 전멸했습니다 |
| 그 외 흑/백 | 흑 승 / 백 승 | 한 쪽 돌이 모두 장외로 나갔습니다 |
| 기권 | (승자 쪽 제목) | 기권으로 승부가 갈렸습니다 |

게임종료: `게임이 종료되었습니다` / `창을 닫아 주세요`. 엔진·턴·실시간 정지 후 `window.close` 시도.

## 9. 사운드·연출

첫 포인터에서 AudioContext unlock. 타구(속도), 튕김, 장외, 턴, 시작, 승패, 기권, 문 여닫기, 당김 긴장음, 긴급 틱, 로비/대기/대전 앰비언스. 충돌 팝, 강한 타구 카메라 흔들림. 액션캠 on이면 장외 클로즈업(킬캠 1.5s). 결과 폭죽(canvas-confetti).

## 10. 제외

텍스트 채팅, 이모지, 토너먼트, 계정·랭킹·매칭 서버, 1:1 공개 매치, 관전 제품화, 대기실 봇 좌석, Vercel, 배경 이미지 업로드(`dotori-alkkagi-scene-bg` 레거시는 부팅 시 삭제).
